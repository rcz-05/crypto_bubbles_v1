import { AuthScreen } from "@/components/AuthScreen";

export const metadata = { title: "Create account · CoinCanvas" };

export default function RegisterPage() {
  return <AuthScreen mode="register" />;
}
